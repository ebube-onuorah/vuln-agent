# Dashboard environment variables
# Copy this to .env.local and fill in your values.

# URL of the FastAPI backend (no trailing slash)
PREDICTOR_API_URL=http://localhost:8000

# Must match API_KEY set on the backend (leave blank if backend has no API_KEY)
PREDICTOR_API_KEY=
